# Baseline

This repository version keeps the executable skill concise and public-safe:

- no source EPUB text is included
- no user-local absolute source path is required
- local source search is optional and configured by `MAOXUAN_EPUB_PATH` or `--epub`
- safety boundaries distinguish virtual game tactics from real-world harm or coercion
